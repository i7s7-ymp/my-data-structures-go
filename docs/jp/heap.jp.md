# ヒープ（Heap）とは

ヒープは「完全二分木」の一種で、各ノードの値がその子ノードの値以上（最大ヒープ）または以下（最小ヒープ）となるように構成されるデータ構造です。主に**優先度付きキュー**や**ヒープソート**などで利用されます。

**このデータ構造の最大の利点は、最小値（または最大値）の高速な取得・削除・挿入が可能な点です。**

# ヒープの構成要素

- **ノード（Node）**: データを保持する単位。親・子の関係を持つ。
- **ルート（Root）**: 最上位のノード。最小ヒープなら最小値、最大ヒープなら最大値を持つ。
- **エッジ（Edge）**: 親子ノードを結ぶ線。
- **葉（Leaf）**: 子ノードを持たないノード。
- **完全二分木**: 全てのレベルが左詰めで埋まっている二分木。

# ヒープの種類

- **最小ヒープ（Min Heap）**: 親 ≤ 子。ルートが最小値。
- **最大ヒープ（Max Heap）**: 親 ≥ 子。ルートが最大値。
- **二項ヒープ・フィボナッチヒープ**: より高度なヒープ構造（主に理論・特殊用途）。

# 二項ヒープ・フィボナッチヒープ

## 二項ヒープ（Binomial Heap）

### 概要

二項ヒープは複数の**二項木**（Binomial Tree）の集合として構成される高度なヒープ構造です。通常の二分ヒープよりも複雑ですが、特にヒープの結合（merge）操作で優れた性能を発揮します。

### 二項木の特徴

```
B₀:  ●

B₁:  ●
     │
     ●


B₂:  ●
    ╱ ╲
   ●   ●
   │
   ●

B₃:    ●
      ╱ ╲
     ●   ●
    ╱ ╲   │
   ●   ●  ●
   │      │
   ●
```

- **B_k**: k 次の二項木（2^k 個のノードを持つ）
- **構造**: B*k は B*{k-1} を 2 つ結合して作成
- **子の数**: ルートノードは k 個の子を持つ
- **高さ**: k（レベル数は k+1）

### 二項ヒープの性質

1. **ヒープ条件**: 各二項木で親 ≤ 子（最小ヒープの場合）
2. **二項木の順序**: 次数の昇順で配列
3. **一意性**: 各次数の二項木は最大 1 つまで
4. **ルートリスト**: 各二項木のルートを連結リストで管理

## フィボナッチヒープ（Fibonacci Heap）

### 概要

フィボナッチヒープは**償却計算量**の観点で最も効率的なヒープ構造の一つです。特にグラフアルゴリズム（ダイクストラ法、プリム法）で威力を発揮しますが、実装が非常に複雑です。

### 構造の特徴

```
ルートリスト（循環連結リスト）:
    [10] ←→ [15] ←→ [8] ←→ [25]
     │       │      │      │
   [20]    [18]   [12]   [30]
     │       │      │
   [25]    [22]   [14]
                     │
                   [16]
```

- **ルートリスト**: すべてのルートノードを循環連結リストで管理
- **任意の木構造**: 各ノードは任意数の子を持てる
- **マーク**: ノードが子を失ったかどうかの印（削除の効率化）
- **最小ポインタ**: 最小値ノードへの直接参照

### フィボナッチ数列との関係

```
フィボナッチ数列: F₀=0, F₁=1, F₂=1, F₃=2, F₄=3, F₅=5, F₆=8, ...

次数 k のノードが持つ最小子ノード数 ≥ F_{k+2} - 1

これにより、n 個のノードを持つヒープの最大次数は O(log n) に制限される
```

## 使用場面

### 二項ヒープが適している場面

- **頻繁なヒープ結合**: 複数の優先度付きキューをマージする場合
- **分散システム**: 異なるノードのヒープを統合
- **並列処理**: ワーカー間でタスクキューを結合
- **メモリ効率**: 通常のヒープより少ないメモリ使用量

### フィボナッチヒープが適している場面

- **グラフアルゴリズム**: ダイクストラ法、プリム法での理論的最適性
- **DecreaseKey 頻発**: キー値の減少操作が非常に頻繁
- **理論研究**: アルゴリズムの理論的下界の証明
- **学術用途**: 計算量理論の研究

## まとめ

### 二項ヒープ

- **利点**: 効率的なマージ操作、理論的に美しい構造
- **欠点**: 実装が複雑、FindMin が O(log n)
- **適用**: ヒープの結合が頻繁な場合

### フィボナッチヒープ

- **利点**: 理論的に最適な償却計算量
- **欠点**: 実装が非常に複雑、定数項が大きい
- **適用**: 主に理論研究、特殊なグラフアルゴリズム

### 実用的な選択指針

1. **一般用途**: 二分ヒープ（配列実装）
2. **頻繁なマージ**: 二項ヒープ
3. **理論的最適性**: フィボナッチヒープ
4. **実装簡易性**: 二分ヒープ

# ヒープの特徴

- **最小/最大値の取得**: O(1)
- **挿入・削除**: O(log n)
- **完全二分木**なので配列で効率的に実装可能
- **順序走査や範囲検索は苦手**

# ヒープと優先度付きキュー

- **ヒープ**
  - **特徴**: データ構造そのもの。完全二分木の特徴を持ち、全ての親ノード・子ノードのペアに対して、親ノードの値が子ノードよりも小さい（最小ヒープ）、または大きい（最大ヒープ）。
  - **用途**: ヒープソート、優先度付きキュー、グラフアルゴリズムでの利用（例: ダイクストラ法、プライム法）
- **優先度付きキュー**
  - **特徴**: 抽象データ型。各要素に優先度を持たせ、ルートノードに優先度が一番高いデータを保存し、高速に取り出せる。
  - **実装**: ヒープが多い。配列や連結リストでも実装可能。
  - **用途**: タスクスケジューリング、イベント駆動システム、グラフアルゴリズム（例: ダイクストラ法、プライム法）

# ヒープの一般的な実装方法

## 配列による実装

- ノードを配列で管理し、インデックスで親子関係を計算
  - 親: `(i-1)/2`
  - 左子: `2i+1`
  - 右子: `2i+2`
- メモリ効率・キャッシュ効率が高い
- 動的サイズ変更も容易

## Go による最小ヒープの実装例

ライブラリを用いずに、自前で最小ヒープを実装します。

```go
package heap

type MinHeap struct {
    data []int
}

func NewMinHeap() *MinHeap {
    return &MinHeap{data: []int{}}
}

func (h *MinHeap) Insert(val int) {
    h.data = append(h.data, val)
    h.upHeap(len(h.data) - 1)
}

func (h *MinHeap) upHeap(i int) {
    for i > 0 {
        parent := (i - 1) / 2
        if h.data[parent] <= h.data[i] {
            break
        }
        h.data[parent], h.data[i] = h.data[i], h.data[parent]
        i = parent
    }
}

func (h *MinHeap) ExtractMin() (int, bool) {
    if len(h.data) == 0 {
        return 0, false
    }
    min := h.data[0]
    last := h.data[len(h.data)-1]
    h.data = h.data[:len(h.data)-1]
    if len(h.data) > 0 {
        h.data[0] = last
        h.downHeap(0)
    }
    return min, true
}

func (h *MinHeap) downHeap(i int) {
    n := len(h.data)
    for {
        left := 2*i + 1
        right := 2*i + 2
        smallest := i
        if left < n && h.data[left] < h.data[smallest] {
            smallest = left
        }
        if right < n && h.data[right] < h.data[smallest] {
            smallest = right
        }
        if smallest == i {
            break
        }
        h.data[i], h.data[smallest] = h.data[smallest], h.data[i]
        i = smallest
    }
}

func (h *MinHeap) Peek() (int, bool) {
    if len(h.data) == 0 {
        return 0, false
    }
    return h.data[0], true
}
```

## Go による最大ヒープの実装例

`container/heap` パッケージを使用して最大ヒープを実装する例です。
優先度の高い要素を効率的に管理できます。

```go
package main

import (
    "container/heap"
    "fmt"
)

// Item はヒープ内の要素を表します
type Item struct {
    value    string // 要素の値
    priority int    // 優先度（大きいほど優先される）
}

// MaxHeap は最大ヒープを表します
type MaxHeap []*Item

// Len はヒープの長さを返します
func (h MaxHeap) Len() int { return len(h) }

// Less は優先度の比較を行います（大きい値が優先される）
func (h MaxHeap) Less(i, j int) bool {
    return h[i].priority > h[j].priority
}

// Swap はヒープ内の要素を交換します
func (h MaxHeap) Swap(i, j int) {
    h[i], h[j] = h[j], h[i]
}

// Push はヒープに新しい要素を追加します
func (h *MaxHeap) Push(x interface{}) {
    *h = append(*h, x.(*Item))
}

// Pop はヒープから最優先の要素を取り出します
func (h *MaxHeap) Pop() interface{} {
    old := *h
    n := len(old)
    item := old[n-1]
    *h = old[0 : n-1]
    return item
}

func main() {
    // 最大ヒープを初期化
    h := &MaxHeap{}
    heap.Init(h)

    // 要素を追加
    heap.Push(h, &Item{value: "task1", priority: 3})
    heap.Push(h, &Item{value: "task2", priority: 5})
    heap.Push(h, &Item{value: "task3", priority: 1})

    // ヒープから要素を取り出し
    for h.Len() > 0 {
        item := heap.Pop(h).(*Item)
        fmt.Printf("Value: %s, Priority: %d\n", item.value, item.priority)
    }
}
```

実行結果

```
Value: task2, Priority: 5
Value: task1, Priority: 3
Value: task3, Priority: 1
```

# 優先度付きキュー（Priority Queue）とは

優先度付きキューは、各要素に「優先度」を持たせ、常に最も優先度の高い（または低い）要素を高速に取り出せるデータ構造です。
ヒープは優先度付きキューの実装に最適です。

## 優先度付きキューの一般的な実装

- **ヒープ（配列実装）**: 挿入・削除ともに O(log n)、最小/最大値の取得が O(1)
- **連結リストや配列**: 挿入または削除が O(n) となり非効率
- **Go の標準ライブラリ**: `container/heap` パッケージで汎用ヒープをサポート

## Go による優先度付きキューの実装例

Go では`container/heap`を使うのが一般的です。
以下は最小ヒープによる優先度付きキューの例です。

```go
package main

import (
    "container/heap"
    "fmt"
)

type Item struct {
    value    string
    priority int
}

type PriorityQueue []*Item

func (pq PriorityQueue) Len() int { return len(pq) }
func (pq PriorityQueue) Less(i, j int) bool {
    return pq[i].priority < pq[j].priority // 最小ヒープ
}
func (pq PriorityQueue) Swap(i, j int) {
    pq[i], pq[j] = pq[j], pq[i]
}

func (pq *PriorityQueue) Push(x interface{}) {
    *pq = append(*pq, x.(*Item))
}

func (pq *PriorityQueue) Pop() interface{} {
    old := *pq
    n := len(old)
    item := old[n-1]
    *pq = old[0 : n-1]
    return item
}

func main() {
    pq := &PriorityQueue{}
    heap.Init(pq)
    heap.Push(pq, &Item{value: "task1", priority: 3})
    heap.Push(pq, &Item{value: "task2", priority: 1})
    heap.Push(pq, &Item{value: "task3", priority: 2})

    for pq.Len() > 0 {
        item := heap.Pop(pq).(*Item)
        fmt.Println(item.value, item.priority)
    }
}
```

# ヒープの応用例

## 1. ソートアルゴリズム・ヒープソート

- **ヒープソート**: 配列をヒープ化してから順次最小/最大値を取り出すソート
- **時間計算量**: O(n log n) - 安定した性能
- **空間計算量**: O(1) - インプレースソート
- **特徴**: 最悪ケースでも O(n log n) が保証される

## 2. 選択問題・k 番目の要素取得

- **k 番目に小さい要素**: 最小ヒープで k 回 ExtractMin を実行
- **k 番目に大きい要素**: 最大ヒープで k 回 ExtractMax を実行
- **トップ k 問題**: 大量データから上位 k 件を効率的に取得
- **メディアン取得**: 2 つのヒープ（最小・最大）を組み合わせて中央値を維持

## 3. グラフアルゴリズム

- **ダイクストラ法**: 最短経路探索で未訪問ノードの距離管理
- **プリム法**: 最小全域木構築でエッジの重み管理
- **A\*アルゴリズム**: 経路探索でのヒューリスティック値管理
- **ハフマン符号化**: 文字頻度に基づく最適符号生成

## 4. データストリーム処理

- **リアルタイム統計**: ストリーミングデータの最小/最大値維持
- **移動窓最小/最大**: 時系列データの窓関数処理
- **オンライン選択**: データ到着順での k 番目要素管理
- **イベント処理**: タイムスタンプ順のイベント管理

## 5. メモリ管理・システムプログラミング

- **メモリプール**: 空きメモリブロックサイズの管理
- **ページ交換アルゴリズム**: LRU 近似でのページ優先度管理
- **ガベージコレクション**: オブジェクトの生存時間管理
- **キャッシュ管理**: アクセス頻度に基づくデータ管理

---

# 優先度付きキューの応用例

## 1. タスクスケジューリング・OS

- **プロセススケジューラ**: CPU 時間の優先度に基づく割り当て
- **リアルタイムシステム**: デッドライン順のタスク実行
- **優先度付きワーカープール**: 重要度に応じたジョブ処理
- **割り込み処理**: 割り込み優先度に基づくハンドラー実行

## 2. ネットワーク・通信システム

- **QoS（Quality of Service）**: パケット優先度に基づく転送
- **帯域制御**: 通信優先度による帯域割り当て
- **ルーティング**: 経路コストに基づく最適経路選択
- **負荷分散**: サーバー負荷に応じたリクエスト分散

## 3. ゲーム開発・シミュレーション

- **イベント駆動シミュレーション**: 時系列イベントの順次処理
- **AI 行動制御**: 行動優先度に基づく NPC 制御
- **リソース管理**: ゲーム内リソースの優先度管理
- **物理演算**: 衝突検出の優先度付き処理

## 4. 医療・緊急システム

- **救急医療**: 患者の重症度（トリアージ）に基づく治療順序
- **手術スケジューリング**: 緊急度と手術時間の最適化
- **薬剤配送**: 緊急度に応じた医薬品配送順序
- **ベッド割り当て**: 患者状態に基づく病床管理

## 5. 金融・取引システム

- **株式取引**: 注文価格と時刻による約定順序管理
- **リスク管理**: リスクレベルに応じたポートフォリオ調整
- **決済処理**: 優先度に基づく決済順序制御
- **アルゴリズム取引**: 取引戦略の優先度管理

## 6. 物流・配送システム

- **配送ルート最適化**: 配送優先度と距離の総合判断
- **在庫管理**: 商品の重要度と在庫レベル管理
- **倉庫作業**: ピッキング作業の優先度管理
- **緊急配送**: 緊急度に応じた配送順序変更

## 7. データ処理・分析

- **バッチ処理**: ジョブ優先度に基づく処理順序
- **ストリーム処理**: データ重要度による処理優先度
- **ETL パイプライン**: データ処理の優先度管理
- **機械学習**: 学習データの重要度付きサンプリング

## 8. Web サービス・アプリケーション

- **API レート制限**: ユーザー優先度による API 呼び出し制御
- **キューイングシステム**: メッセージ処理の優先度管理
- **コンテンツ配信**: ユーザー重要度による配信優先度
- **通知システム**: 通知の緊急度による送信順序

## 9. 検索・情報取得

- **検索エンジン**: 検索結果の関連度による順位付け
- **推薦システム**: ユーザー嗜好の重み付き推薦
- **コンテンツランキング**: 人気度と鮮度による順位決定
- **情報フィルタリング**: 情報の重要度による表示制御

## 10. IoT・センサーネットワーク

- **センサーデータ処理**: データ重要度による処理優先度
- **エネルギー管理**: バッテリー状態による通信優先度
- **故障検知**: 異常レベルによるアラート優先度
- **デバイス制御**: 制御コマンドの緊急度管理

## 11. コンテンツ管理・メディア

- **ライブストリーミング**: フレーム重要度による送信制御
- **コンテンツ配信**: 視聴者数による配信優先度
- **メディア処理**: 処理タスクの優先度管理
- **アーカイブシステム**: データ重要度による保存優先度

## 12. 教育・学習システム

- **学習管理**: 学習項目の優先度による推奨順序
- **課題評価**: 提出期限と重要度による採点順序
- **リソース配布**: 教材の重要度による配信管理
- **進捗管理**: 学習進捗の優先度管理

# ヒープと他のデータ構造との比較

## 全データ構造比較

| 特徴                 | ヒープ              | 二分探索木      | 平衡木           | 配列                | ハッシュテーブル | 連結リスト        | スタック      | キュー           |
| -------------------- | ------------------- | --------------- | ---------------- | ------------------- | ---------------- | ----------------- | ------------- | ---------------- |
| **最小/最大値取得**  | ✅ O(1)             | ⚠️ O(log n)     | ⚠️ O(log n)      | ✅ O(1)ソート済     | ❌ O(n)          | ❌ O(n)           | ✅ O(1)トップ | ✅ O(1)先頭      |
| **挿入性能**         | ✅ O(log n)         | ✅ O(log n)平均 | ⚠️ O(log n)+回転 | ❌ O(n)             | ✅ O(1)平均      | ✅ O(1)           | ✅ O(1)       | ✅ O(1)          |
| **削除性能**         | ✅ O(log n)         | ✅ O(log n)平均 | ⚠️ O(log n)+回転 | ❌ O(n)             | ✅ O(1)平均      | ✅ O(1)           | ✅ O(1)       | ✅ O(1)          |
| **検索性能**         | ❌ O(n)             | ✅ O(log n)平均 | ✅ O(log n)保証  | ⚠️ O(log n)ソート済 | ✅ O(1)平均      | ❌ O(n)           | ❌ O(n)       | ❌ O(n)          |
| **順序維持**         | ❌ ヒープ順序のみ   | ✅ 自動ソート   | ✅ 自動ソート    | ✅ インデックス順   | ❌ ハッシュ順    | ✅ 挿入順         | ✅ LIFO       | ✅ FIFO          |
| **メモリ効率**       | ✅ 最高（配列実装） | ✅ 良好         | ⚠️ 追加情報必要  | ✅ 最高             | ⚠️ 負荷率依存    | ⚠️ ポインタ分重い | ✅ 良好       | ✅ 良好          |
| **範囲検索**         | ❌ 非効率           | ✅ 効率的       | ✅ 効率的        | ⚠️ 線形探索         | ❌ 不適          | ❌ 線形探索       | ❌ 不適       | ❌ 不適          |
| **キャッシュ効率**   | ✅ 非常に高い       | ⚠️ ポインタ追跡 | ⚠️ ポインタ追跡  | ✅ 最高             | ⚠️ 実装依存      | ❌ 低い           | ✅ 高い       | ✅ 高い          |
| **実装複雑度**       | ✅ シンプル         | ⚠️ 中程度       | ❌ 複雑          | ✅ 最も簡単         | ⚠️ 中程度        | ✅ 簡単           | ✅ 最も簡単   | ✅ 最も簡単      |
| **動的サイズ変更**   | ✅ 容易             | ✅ 容易         | ✅ 自動調整      | ⚠️ 再配置必要       | ✅ 容易          | ✅ 容易           | ✅ 容易       | ✅ 容易          |
| **並行処理**         | ⚠️ 同期必要         | ⚠️ 注意必要     | ⚠️ 注意必要      | ✅ 比較的容易       | ⚠️ 同期必要      | ❌ 困難           | ⚠️ 同期必要   | ⚠️ 同期必要      |
| **ランダムアクセス** | ❌ 不適             | ❌ O(log n)     | ❌ O(log n)      | ✅ O(1)             | ✅ O(1)平均      | ❌ O(n)           | ❌ トップのみ | ❌ 先頭/末尾のみ |

## 使用場面別最適選択

| 使用場面                     | 第 1 選択          | 第 2 選択        | 第 3 選択        | 避けるべき                   |
| ---------------------------- | ------------------ | ---------------- | ---------------- | ---------------------------- |
| **優先度付きキュー**         | ヒープ             | 平衡二分探索木   | -                | 配列、ハッシュテーブル       |
| **タスクスケジューリング**   | ヒープ             | 平衡二分探索木   | -                | 連結リスト、スタック         |
| **ソートアルゴリズム**       | ヒープ             | 配列             | 平衡二分探索木   | ハッシュテーブル、連結リスト |
| **最小/最大値管理**          | ヒープ             | 平衡二分探索木   | -                | 配列、連結リスト             |
| **リアルタイムシステム**     | ヒープ             | 平衡木（赤黒木） | 配列             | グラフ、連結リスト           |
| **イベント駆動処理**         | ヒープ             | -                | -                | 配列、ハッシュテーブル       |
| **一般的な検索・挿入・削除** | 平衡二分探索木     | ハッシュテーブル | 二分探索木       | ヒープ                       |
| **データベースインデックス** | B 木・B+木         | 平衡二分探索木   | ハッシュテーブル | ヒープ、配列                 |
| **文字列検索・辞書**         | トライ木           | ハッシュテーブル | 平衡二分探索木   | ヒープ、配列                 |
| **ファイルシステム**         | 一般木             | B 木             | -                | ヒープ、配列                 |
| **数値検索・ソート**         | 平衡二分探索木     | ヒープ           | 配列             | 連結リスト、グラフ           |
| **範囲クエリ**               | セグメント木       | 平衡二分探索木   | 配列             | ヒープ、ハッシュテーブル     |
| **メモリ制約環境**           | ヒープ（配列実装） | 二分探索木       | 配列             | グラフ、ハッシュテーブル     |
| **大量データ処理**           | B 木               | 平衡二分探索木   | ハッシュテーブル | ヒープ、配列                 |
| **頻繁な挿入・削除**         | 赤黒木             | ヒープ           | ハッシュテーブル | 配列                         |
| **読み取り重視**             | AVL 木             | B 木             | 配列             | ヒープ、連結リスト           |
| **グラフアルゴリズム**       | ヒープ             | 平衡二分探索木   | -                | 配列、連結リスト             |
| **機械学習・AI**             | 決定木             | ヒープ           | 配列             | 連結リスト、スタック         |

# パフォーマンス特性

## ヒープ種別によるパフォーマンス比較

| 操作パターン       | 最小ヒープ    | 最大ヒープ    | 二項ヒープ  | フィボナッチヒープ | 配列実装    | ポインタ実装  |
| ------------------ | ------------- | ------------- | ----------- | ------------------ | ----------- | ------------- |
| **最小値取得**     | ✅ O(1)       | ❌ 最大値のみ | ✅ O(1)     | ✅ O(1)            | ✅ O(1)     | ✅ O(1)       |
| **最大値取得**     | ❌ 最小値のみ | ✅ O(1)       | ✅ O(1)     | ✅ O(1)            | ✅ O(1)     | ✅ O(1)       |
| **挿入**           | ✅ O(log n)   | ✅ O(log n)   | ✅ O(log n) | ✅ O(1)償却        | ✅ O(log n) | ✅ O(log n)   |
| **削除**           | ✅ O(log n)   | ✅ O(log n)   | ✅ O(log n) | ✅ O(log n)償却    | ✅ O(log n) | ✅ O(log n)   |
| **検索**           | ❌ O(n)       | ❌ O(n)       | ❌ O(n)     | ❌ O(n)            | ❌ O(n)     | ❌ O(n)       |
| **構築**           | ✅ O(n)       | ✅ O(n)       | ✅ O(n)     | ✅ O(n)            | ✅ O(n)     | ✅ O(n)       |
| **メモリ使用量**   | ✅ 最小       | ✅ 最小       | ⚠️ ポインタ | ⚠️ 複雑構造        | ✅ 最小     | ⚠️ ポインタ分 |
| **キャッシュ効率** | ✅ 高い       | ✅ 高い       | ⚠️ 中程度   | ❌ 低い            | ✅ 最高     | ❌ 低い       |
| **実装複雑度**     | ✅ 簡単       | ✅ 簡単       | ⚠️ 中程度   | ❌ 複雑            | ✅ 簡単     | ⚠️ 中程度     |
| **並列化適性**     | ⚠️ 制限あり   | ⚠️ 制限あり   | ⚠️ 制限あり | ❌ 困難            | ⚠️ 制限あり | ❌ 困難       |

# ヒープが最適な場面と避けるべき場面

## ✅ ヒープが最適な場面

- **優先度付きキュー**: 最小/最大値の高速取得が必要
- **タスクスケジューリング**: 優先度に基づく処理順序制御
- **ヒープソート**: O(n log n)の安定したソート性能
- **グラフアルゴリズム**: ダイクストラ法、プリム法での距離管理
- **イベント駆動システム**: 時刻順のイベント処理
- **リアルタイム処理**: 最悪ケース性能保証が必要
- **メモリ制約環境**: 最小メモリでの効率的処理

## ❌ ヒープを避けるべき場面

- **一般的な検索**: O(n)で非効率
- **範囲クエリ**: 範囲検索が主目的
- **順序走査**: ソート済み順序での全要素取得
- **ランダムアクセス**: インデックスによる直接アクセス
- **文字列処理**: プレフィックス検索やパターンマッチング
- **関係性管理**: ノード間の複雑な関係表現
- **頻繁な検索**: 特定要素の存在確認が主目的

# まとめ

- ヒープは完全二分木を配列で実装し、最小/最大値の高速な取得・削除・挿入が可能
- 優先度付きキューはヒープで効率的に実装できる
- Go では`container/heap`パッケージを使うことで、柔軟な優先度付きキューを簡単に実装できる
